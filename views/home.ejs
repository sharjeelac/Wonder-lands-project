<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wonder Lands - Travel Paradise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --gradient: linear-gradient(135deg, #6366f1, #8b5cf6, #ec4899);
        }

        body {
            font-family: 'Segoe UI', system-ui, sans-serif;
            overflow-x: hidden;
        }

        .hero {
            min-height: 100vh;
            background: var(--gradient);
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .container {
            margin-top: 160px;
        }

        .animated-card {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .animated-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 25px 50px rgba(0,0,0,0.2);
        }

        .explore-btn {
            background: white;
            color: #6366f1;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 700;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: none;
        }

        .explore-btn:hover {
            letter-spacing: 2px;
            transform: scale(1.05);
        }

        .floating {
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body>

<!-- Hero Section -->
<section class="hero text-white">
    <div class="container h-100">
        <div class="row h-100 align-items-center  hero-content">
            <div class="col-12 text-center">
                <h1 class="display-1 fw-bold mb-4">Discover Hidden<br>World Wonders</h1>
                <p class="lead fs-3 mb-5">Experience stays that redefine adventure</p>
                <a href="/listings" class="explore-btn btn btn-lg">
                    Explore Listings <i class="fas fa-arrow-right ms-2"></i>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Features Grid -->
<section class="py-5 bg-light">
    <div class="container py-5">
        <div class="row g-5">
            <div class="col-md-4">
                <div class="animated-card p-4 text-center">
                    <div class="floating">
                        <img src="https://images.unsplash.com/photo-1587502537104-aac10f5fb6f7?auto=format&fit=crop&w=600" 
                             class="img-fluid rounded-3 mb-3" 
                             alt="Mountain">
                    </div>
                    <h3>Mountain Escapes</h3>
                    <p class="text-muted">Breathtaking alpine retreats</p>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="animated-card p-4 text-center">
                    <div class="floating">
                        <img src="https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=600" 
                             class="img-fluid rounded-3 mb-3" 
                             alt="Beach">
                    </div>
                    <h3>Beach Paradises</h3>
                    <p class="text-muted">Crystal-clear waters await</p>
                </div>
            </div>

            <div class="col-md-4">
                <div class="animated-card p-4 text-center">
                    <div class="floating">
                        <img src="https://images.unsplash.com/photo-1483728642387-6c3bdd6c93e5?auto=format&fit=crop&w=600" 
                             class="img-fluid rounded-3 mb-3" 
                             alt="Desert">
                    </div>
                    <h3>Desert Retreats</h3>
                    <p class="text-muted">Surreal sandy landscapes</p>
                </div>
            </div>
        </div>
    </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>